<template>
  <div class="cyberpunk-landing">
    <div class="scanlines"></div>
    <div class="matrix-bg"></div>
    
    <!-- Navigation -->
    <nav class="cyber-nav">
      <div class="nav-container">
        <div class="nav-brand">
          <span class="glitch-text" data-text="OMNI_INSTA">OMNI_INSTA</span>
          <div class="brand-line"></div>
        </div>
        <div class="nav-links">
          <a href="#features" class="nav-link">FEATURES</a>
          <a href="#pricing" class="nav-link">PRICING</a>
          <a href="#contact" class="nav-link">CONTACT</a>
          <button class="cyber-btn login-btn">LOGIN</button>
        </div>
      </div>
    </nav>

    <!-- Hero Section -->
    <section class="hero-section">
      <div class="hero-grid">
        <div class="hero-left">
          <div class="terminal-window">
            <div class="terminal-header">
              <div class="terminal-buttons">
                <span class="terminal-btn close"></span>
                <span class="terminal-btn minimize"></span>
                <span class="terminal-btn maximize"></span>
              </div>
              <span class="terminal-title">neural_interface.exe</span>
            </div>
            <div class="terminal-body">
              <div class="terminal-line">
                <span class="prompt">root@matrix:~$</span>
                <span class="typing-animation">initialize neural_posting_system</span>
              </div>
              <div class="terminal-line">
                <span class="success">[OK] Neural Link Established</span>
              </div>
              <div class="terminal-line">
                <span class="success">[OK] Instagram API Connected</span>
              </div>
              <div class="terminal-line">
                <span class="success">[OK] AI Scheduling Active</span>
              </div>
              <div class="terminal-line">
                <span class="warning">[SYSTEM] Ready for Content Upload</span>
              </div>
            </div>
          </div>
        </div>
        
        <div class="hero-right">
          <h1 class="hero-title">
            <span class="neon-text">NEURAL</span>
            <span class="glitch-text" data-text="INSTAGRAM">INSTAGRAM</span>
            <span class="neon-text">AUTOMATION</span>
          </h1>
          
          <p class="hero-subtitle">
            Interface with the Instagram matrix. Schedule, automate, and dominate 
            the social network through advanced AI-powered posting algorithms.
          </p>
          
          <div class="cta-buttons">
            <button class="cyber-btn primary-cta">
              <span class="btn-text">JACK IN NOW</span>
              <div class="btn-glow"></div>
              <div class="btn-particles"></div>
            </button>
            <button class="cyber-btn secondary-cta">
              <span class="btn-text">VIEW DEMO</span>
              <div class="hologram-effect"></div>
            </button>
          </div>
          
          <div class="stats-grid">
            <div class="stat-card">
              <div class="stat-number neon-cyan">1M+</div>
              <div class="stat-label">POSTS AUTOMATED</div>
            </div>
            <div class="stat-card">
              <div class="stat-number neon-magenta">50K+</div>
              <div class="stat-label">NEURAL LINKS</div>
            </div>
            <div class="stat-card">
              <div class="stat-number neon-green">99.9%</div>
              <div class="stat-label">UPTIME</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Features Section -->
    <section class="features-section" id="features">
      <div class="section-header">
        <h2 class="section-title neon-text">NEURAL CAPABILITIES</h2>
        <div class="hex-pattern"></div>
      </div>
      
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <div class="hologram-cube"></div>
          </div>
          <h3 class="feature-title">AI SCHEDULING</h3>
          <p class="feature-description">
            Advanced neural algorithms analyze engagement patterns and 
            automatically schedule posts for maximum viral potential.
          </p>
          <div class="feature-stats">
            <span class="stat">+300% Engagement</span>
          </div>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <div class="circuit-pattern"></div>
          </div>
          <h3 class="feature-title">MULTI-ACCOUNT MATRIX</h3>
          <p class="feature-description">
            Simultaneously control multiple Instagram accounts through 
            a single neural interface. Scale your digital presence.
          </p>
          <div class="feature-stats">
            <span class="stat">∞ Accounts</span>
          </div>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <div class="data-stream"></div>
          </div>
          <h3 class="feature-title">CONTENT SYNTHESIS</h3>
          <p class="feature-description">
            AI-powered content optimization with real-time hashtag 
            analysis and engagement prediction algorithms.
          </p>
          <div class="feature-stats">
            <span class="stat">Real-time Analysis</span>
          </div>
        </div>
      </div>
    </section>

    <!-- Pricing Section -->
    <section class="pricing-section" id="pricing">
      <div class="section-header">
        <h2 class="section-title neon-text">ACCESS LEVELS</h2>
        <p class="section-subtitle">Choose your neural interface package</p>
      </div>
      
      <div class="pricing-grid">
        <div class="price-card basic">
          <div class="card-header">
            <h3 class="plan-name">INITIATE</h3>
            <div class="plan-price">
              <span class="currency">¥</span>
              <span class="amount">999</span>
              <span class="period">/month</span>
            </div>
          </div>
          <div class="card-features">
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>3 Neural Links</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>100 Automated Posts</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>Basic AI Scheduling</span>
            </div>
          </div>
          <button class="cyber-btn plan-btn">SELECT PACKAGE</button>
        </div>
        
        <div class="price-card pro featured">
          <div class="featured-badge">MOST POPULAR</div>
          <div class="card-header">
            <h3 class="plan-name">HACKER</h3>
            <div class="plan-price">
              <span class="currency">¥</span>
              <span class="amount">2999</span>
              <span class="period">/month</span>
            </div>
          </div>
          <div class="card-features">
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>10 Neural Links</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>1000 Automated Posts</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>Advanced AI Analytics</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>Priority Neural Bandwidth</span>
            </div>
          </div>
          <button class="cyber-btn plan-btn primary">JACK IN NOW</button>
        </div>
        
        <div class="price-card enterprise">
          <div class="card-header">
            <h3 class="plan-name">ARCHITECT</h3>
            <div class="plan-price">
              <span class="currency">¥</span>
              <span class="amount">9999</span>
              <span class="period">/month</span>
            </div>
          </div>
          <div class="card-features">
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>∞ Neural Links</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>10K Automated Posts</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>Neural Network Priority</span>
            </div>
            <div class="feature-item">
              <span class="check-icon">✓</span>
              <span>Custom AI Training</span>
            </div>
          </div>
          <button class="cyber-btn plan-btn">CONTACT MATRIX</button>
        </div>
      </div>
    </section>
  </div>
</template>

<script setup lang="ts">
import { onMounted, onUnmounted } from 'vue'

// Glitch effect for text elements
const initGlitchEffect = () => {
  const glitchElements = document.querySelectorAll('.glitch-text')
  
  glitchElements.forEach(element => {
    setInterval(() => {
      if (Math.random() > 0.95) {
        element.classList.add('glitching')
        setTimeout(() => {
          element.classList.remove('glitching')
        }, 200)
      }
    }, 100)
  })
}

// Matrix background animation
const initMatrixBg = () => {
  const canvas = document.createElement('canvas')
  const ctx = canvas.getContext('2d')
  const matrixBg = document.querySelector('.matrix-bg')
  
  if (!ctx || !matrixBg) return
  
  canvas.width = window.innerWidth
  canvas.height = window.innerHeight
  matrixBg.appendChild(canvas)
  
  const matrix = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ123456789@#$%^&*()*&^%+-/~{[|`]}'
  const drops: number[] = []
  
  for (let x = 0; x < canvas.width / 10; x++) {
    drops[x] = 1
  }
  
  const draw = () => {
    ctx.fillStyle = 'rgba(0, 0, 0, 0.04)'
    ctx.fillRect(0, 0, canvas.width, canvas.height)
    
    ctx.fillStyle = '#00ff00'
    ctx.font = '10px Orbitron'
    
    for (let i = 0; i < drops.length; i++) {
      const text = matrix[Math.floor(Math.random() * matrix.length)]
      ctx.fillText(text, i * 10, drops[i] * 10)
      
      if (drops[i] * 10 > canvas.height && Math.random() > 0.975) {
        drops[i] = 0
      }
      drops[i]++
    }
  }
  
  const interval = setInterval(draw, 35)
  
  return () => {
    clearInterval(interval)
    canvas.remove()
  }
}

onMounted(() => {
  initGlitchEffect()
  const cleanupMatrix = initMatrixBg()
  
  onUnmounted(() => {
    cleanupMatrix && cleanupMatrix()
  })
})
</script>

<style scoped>
@import url('https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&display=swap');

/* Base Styles */
.cyberpunk-landing {
  min-height: 100vh;
  background: #000;
  color: #00ffff;
  font-family: 'Orbitron', monospace;
  position: relative;
  overflow-x: hidden;
}

/* Scanlines Effect */
.scanlines {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: linear-gradient(
    to bottom,
    transparent 50%,
    rgba(0, 255, 255, 0.03) 51%
  );
  background-size: 100% 4px;
  pointer-events: none;
  z-index: 1;
}

/* Matrix Background */
.matrix-bg {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 0;
  opacity: 0.1;
}

/* Navigation */
.cyber-nav {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  background: rgba(0, 0, 0, 0.9);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid #00ffff;
  z-index: 100;
}

.nav-container {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 2rem;
  max-width: 1400px;
  margin: 0 auto;
}

.nav-brand {
  display: flex;
  align-items: center;
  gap: 1rem;
}

.brand-line {
  width: 60px;
  height: 2px;
  background: linear-gradient(90deg, #00ffff, #ff00ff);
  animation: pulse 2s infinite;
}

.nav-links {
  display: flex;
  align-items: center;
  gap: 2rem;
}

.nav-link {
  color: #00ffff;
  text-decoration: none;
  font-weight: 700;
  transition: all 0.3s ease;
  position: relative;
}

.nav-link:hover {
  color: #ff00ff;
  text-shadow: 0 0 10px #ff00ff;
}

/* Hero Section */
.hero-section {
  padding: 120px 2rem 80px;
  position: relative;
  z-index: 2;
}

.hero-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  max-width: 1400px;
  margin: 0 auto;
  align-items: center;
}

/* Terminal Window */
.terminal-window {
  background: rgba(0, 0, 0, 0.9);
  border: 1px solid #00ffff;
  border-radius: 8px;
  box-shadow: 0 0 30px rgba(0, 255, 255, 0.3);
}

.terminal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 0.5rem 1rem;
  background: rgba(0, 255, 255, 0.1);
  border-bottom: 1px solid #00ffff;
}

.terminal-buttons {
  display: flex;
  gap: 0.5rem;
}

.terminal-btn {
  width: 12px;
  height: 12px;
  border-radius: 50%;
}

.terminal-btn.close { background: #ff0040; }
.terminal-btn.minimize { background: #ffff00; }
.terminal-btn.maximize { background: #00ff00; }

.terminal-title {
  color: #00ffff;
  font-size: 0.9rem;
  font-weight: 700;
}

.terminal-body {
  padding: 1.5rem;
  font-family: 'Courier New', monospace;
}

.terminal-line {
  margin-bottom: 0.5rem;
  font-size: 0.9rem;
}

.prompt {
  color: #00ff00;
  margin-right: 0.5rem;
}

.typing-animation {
  color: #00ffff;
  animation: typing 3s infinite;
}

.success { color: #00ff00; }
.warning { color: #ffff00; }

/* Hero Content */
.hero-title {
  font-size: 3.5rem;
  font-weight: 900;
  line-height: 1.1;
  margin-bottom: 1.5rem;
  display: flex;
  flex-direction: column;
}

.hero-subtitle {
  font-size: 1.2rem;
  color: rgba(0, 255, 255, 0.8);
  margin-bottom: 2rem;
  line-height: 1.6;
}

/* CTA Buttons */
.cta-buttons {
  display: flex;
  gap: 1.5rem;
  margin-bottom: 3rem;
}

.cyber-btn {
  position: relative;
  padding: 1rem 2rem;
  background: transparent;
  border: 2px solid #00ffff;
  color: #00ffff;
  font-family: 'Orbitron', monospace;
  font-weight: 700;
  text-transform: uppercase;
  cursor: pointer;
  transition: all 0.3s ease;
  overflow: hidden;
}

.cyber-btn:hover {
  background: rgba(0, 255, 255, 0.1);
  box-shadow: 0 0 30px rgba(0, 255, 255, 0.5);
}

.primary-cta {
  border-color: #ff00ff;
  color: #ff00ff;
}

.primary-cta:hover {
  background: rgba(255, 0, 255, 0.1);
  box-shadow: 0 0 30px rgba(255, 0, 255, 0.5);
}

.login-btn {
  padding: 0.5rem 1.5rem;
  font-size: 0.9rem;
}

/* Stats Grid */
.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 2rem;
}

.stat-card {
  text-align: center;
  padding: 1.5rem;
  border: 1px solid rgba(0, 255, 255, 0.3);
  background: rgba(0, 0, 0, 0.5);
}

.stat-number {
  font-size: 2.5rem;
  font-weight: 900;
  margin-bottom: 0.5rem;
}

.stat-label {
  font-size: 0.8rem;
  color: rgba(0, 255, 255, 0.7);
  letter-spacing: 2px;
}

/* Features Section */
.features-section {
  padding: 80px 2rem;
  position: relative;
  z-index: 2;
}

.section-header {
  text-align: center;
  margin-bottom: 4rem;
}

.section-title {
  font-size: 3rem;
  font-weight: 900;
  margin-bottom: 1rem;
}

.features-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
  gap: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}

.feature-card {
  padding: 2rem;
  border: 1px solid #00ffff;
  background: rgba(0, 0, 0, 0.8);
  transition: all 0.3s ease;
  position: relative;
}

.feature-card:hover {
  border-color: #ff00ff;
  box-shadow: 0 0 30px rgba(255, 0, 255, 0.3);
  transform: translateY(-5px);
}

.feature-title {
  font-size: 1.5rem;
  font-weight: 700;
  margin: 1rem 0;
  color: #00ffff;
}

.feature-description {
  color: rgba(0, 255, 255, 0.8);
  line-height: 1.6;
  margin-bottom: 1rem;
}

/* Pricing Section */
.pricing-section {
  padding: 80px 2rem;
  position: relative;
  z-index: 2;
}

.pricing-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 2rem;
  max-width: 1000px;
  margin: 0 auto;
}

.price-card {
  border: 1px solid #00ffff;
  background: rgba(0, 0, 0, 0.9);
  padding: 2rem;
  position: relative;
  transition: all 0.3s ease;
}

.price-card.featured {
  border-color: #ff00ff;
  transform: scale(1.05);
}

.featured-badge {
  position: absolute;
  top: -1rem;
  left: 50%;
  transform: translateX(-50%);
  background: #ff00ff;
  color: #000;
  padding: 0.5rem 1rem;
  font-weight: 700;
  font-size: 0.8rem;
}

.plan-name {
  font-size: 1.5rem;
  font-weight: 700;
  color: #00ffff;
  margin-bottom: 1rem;
}

.plan-price {
  display: flex;
  align-items: baseline;
  margin-bottom: 2rem;
}

.amount {
  font-size: 3rem;
  font-weight: 900;
  color: #ff00ff;
}

.currency, .period {
  color: rgba(0, 255, 255, 0.7);
}

.feature-item {
  display: flex;
  align-items: center;
  gap: 1rem;
  margin-bottom: 1rem;
  color: rgba(0, 255, 255, 0.9);
}

.check-icon {
  color: #00ff00;
  font-weight: 700;
}

.plan-btn {
  width: 100%;
  margin-top: 2rem;
}

/* Neon Text Effects */
.neon-text {
  color: #00ffff;
  text-shadow: 
    0 0 5px #00ffff,
    0 0 10px #00ffff,
    0 0 20px #00ffff;
  animation: neon-flicker 2s infinite alternate;
}

.neon-cyan {
  color: #00ffff;
  text-shadow: 0 0 10px #00ffff;
}

.neon-magenta {
  color: #ff00ff;
  text-shadow: 0 0 10px #ff00ff;
}

.neon-green {
  color: #00ff00;
  text-shadow: 0 0 10px #00ff00;
}

/* Glitch Effect */
.glitch-text {
  position: relative;
  color: #00ffff;
}

.glitch-text::before,
.glitch-text::after {
  content: attr(data-text);
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  opacity: 0;
}

.glitch-text.glitching::before {
  animation: glitch-1 0.1s infinite;
  color: #ff00ff;
  opacity: 1;
}

.glitch-text.glitching::after {
  animation: glitch-2 0.1s infinite;
  color: #00ff00;
  opacity: 1;
}

/* Animations */
@keyframes typing {
  0%, 50% { opacity: 1; }
  51%, 100% { opacity: 0; }
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

@keyframes neon-flicker {
  0%, 100% { text-shadow: 0 0 5px #00ffff, 0 0 10px #00ffff, 0 0 20px #00ffff; }
  50% { text-shadow: 0 0 2px #00ffff, 0 0 5px #00ffff, 0 0 10px #00ffff; }
}

@keyframes glitch-1 {
  0% { transform: translate(0); }
  20% { transform: translate(-2px, 2px); }
  40% { transform: translate(-2px, -2px); }
  60% { transform: translate(2px, 2px); }
  80% { transform: translate(2px, -2px); }
  100% { transform: translate(0); }
}

@keyframes glitch-2 {
  0% { transform: translate(0); }
  20% { transform: translate(2px, -2px); }
  40% { transform: translate(2px, 2px); }
  60% { transform: translate(-2px, -2px); }
  80% { transform: translate(-2px, 2px); }
  100% { transform: translate(0); }
}

/* Responsive Design */
@media (max-width: 768px) {
  .hero-grid {
    grid-template-columns: 1fr;
    gap: 2rem;
  }
  
  .hero-title {
    font-size: 2.5rem;
  }
  
  .cta-buttons {
    flex-direction: column;
  }
  
  .stats-grid {
    grid-template-columns: 1fr;
  }
  
  .nav-links {
    display: none;
  }
}
</style>